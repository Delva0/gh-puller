[project]
name = "gh-puller-webui-api"
version = "0.1.0"
description = "DeepWiki 兼容后端 HTTP 服务(端点层);引擎与任务复用 gh_puller"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.2.142",
    "fastapi>=0.139.0",
    "gh-puller",
    "mcp>=2.1.1",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.51.0",
    "websockets>=16.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

# 平铺布局多顶层模块,须显式声明,否则 setuptools 自动发现拒绝构建
[tool.setuptools]
py-modules = ["app", "generators", "schemas", "tasks"]

[[tool.uv.index]]
url = "https://pypi.org/simple"

[tool.uv.sources]
gh-puller = { path = "../../..", editable = true }

[dependency-groups]
dev = [
    "httpx",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "RUF001", "RUF002", "RUF003",
    "D415", "D403",
    "D101", "D102", "D103", "D105", "D107",
    "ANN",
    "ARG", "SLF001", "BLE001", "PLC0415", "TID252", "PTH", "FBT",
    "EM", "TRY003", "T201", "TRY301",
    "PLW0602", "PLW0603", "S603", "S607",
    "N803", "N806", "N815",
    "C901", "PLR0911", "PLR0912", "PLR0913", "PLR0915", "PLR0917", "PLR2004",
    "CPY001", "TD", "FIX", "PT018",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S108", "INP001", "D100", "D104"]
