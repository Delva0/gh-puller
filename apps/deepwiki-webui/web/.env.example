# 后端地址（服务端代理/rewrites 用；默认 http://localhost:8001）
# 注：浏览器端代码不允许依赖 SERVER_BASE_URL（不会被内联）
# SERVER_BASE_URL=http://localhost:8001

# 浏览器直连后端 WS 的地址（不设时按 host:NEXT_PUBLIC_API_PORT 推导）
# NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8001
# 浏览器直连后端 HTTP/WS 端口（默认 8001，与后端 PORT 联动）
# NEXT_PUBLIC_API_PORT=8001
