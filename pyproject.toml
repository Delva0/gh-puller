[project]
name = "gh-puller"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["httpx", "python-dateutil", "python-dotenv"]


[tool.ruff]
line-length = 120
